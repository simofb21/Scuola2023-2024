#include <stdio.h>

int main(){
    int ia;
    short as;
    long la;
    char ci;
    printf("\nla variabile as occupa %d byte ",sizeof(as));
    printf("\nla variabile ia occupa %d byte ",sizeof(ia));
    printf("\nla variabile la occupa %d byte ",sizeof(la));
    printf("\nla variabile c occupa %d byte ",sizeof(ci));}